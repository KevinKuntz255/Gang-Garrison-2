<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<event category="CREATE" id="0">
  <actions>
    <action id="603" library="1">
      <!--action name: Code-->
      <kind>CODE</kind>
      <allowRelative>false</allowRelative>
      <question>false</question>
      <canApplyTo>true</canApplyTo>
      <actionType>CODE</actionType>
      <functionName/>
      <relative>false</relative>
      <not>false</not>
      <appliesTo>.self</appliesTo>
      <arguments>
        <argument kind="STRING">{
    xoffset = -33;
    yoffset = -40;
    refireTime = 18;
    
    reloadTime = 15;
    reloadBuffer = 26;
    maxAmmo = 2;
    event_inherited();
    
    destructProjectilesOnDeath = false; // hehehehehehehehehehehehehehehehehe
    
    ammoCount = owner.ammo[activeWeapon];
    idle=true;
    
    damSource = DAMAGE_SOURCE_GRENADE;

    isMelee = true;
    
    if (ammoCount &lt; maxAmmo) alarm[5] = (reloadBuffer + reloadTime) / global.delta_factor;
    
    weaponGrade = WGRADE_UNIQUE;
    weaponType = WTYPE_PROJECTILE;
    /* TODO: implement ability instead of using time/charge
    until then, added a lil SpecialHUD fix
    ability = ABILITY_CHARGEDSHOT;
    abilityBuffer = AACTIVATE_HOLDM1;
    abilityName = "CHARGE";
    meter = 0;
    maxMeter = 89;*/
    time = 0;
    charge = false;
    ability = 50; // dummy variable
    abilityName = "CHARGE"
    
    normalSprite = GrenadehandS;
    recoilSprite = GrenadehandFS;
    reloadSprite = GrenadehandFRS;

    sprite_index = normalSprite;

    recoilTime = refireTime;
    recoilAnimLength = sprite_get_number(recoilSprite) / 2;
    recoilImageSpeed = recoilAnimLength / recoilTime;

    reloadAnimLength = sprite_get_number(reloadSprite) / 2;
    reloadImageSpeed = reloadAnimLength / reloadTime;
}
</argument>
      </arguments>
    </action>
  </actions>
</event>
