<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<event category="CREATE" id="0">
  <actions>
    <action id="603" library="1">
      <!--action name: Code-->
      <kind>CODE</kind>
      <allowRelative>false</allowRelative>
      <question>false</question>
      <canApplyTo>true</canApplyTo>
      <actionType>CODE</actionType>
      <functionName/>
      <relative>false</relative>
      <not>false</not>
      <appliesTo>.self</appliesTo>
      <arguments>
        <argument kind="STRING">{
    xoffset= -3;
    yoffset= -2;
    refireTime = 18;
    event_inherited();

    maxAmmo = 1;
    ammoCount = maxAmmo;
    reloadTime = 300;
    reloadBuffer = refireTime;
    idle=true;
    
    isMelee = true;
    
    // used to be INVIS, maybe use that later?
    ability = INVINC;
    abilityName = "BONK";
    abilityVisual = "BLUR";
    rechargeAbility = ALARM;
    
    owner.eatCooldown = 900;
    maxMeter = (1-(owner.alarm[6]/(owner.eatCooldown / global.delta_factor))) * 100
    meter = maxMeter;
    
    weaponGrade = UNIQUE;
    weaponType = CONSUMABLE;
    
    normalSprite = BonkHandS;
    recoilSprite = BonkHandS;
    reloadSprite = BonkHandS;

    sprite_index = normalSprite;

    image_speed = 0;

    recoilTime = refireTime;
    recoilAnimLength = sprite_get_number(recoilSprite) / 2;
    recoilImageSpeed = recoilAnimLength / recoilTime;

    reloadAnimLength = sprite_get_number(reloadSprite) / 2;
    reloadImageSpeed = reloadAnimLength / reloadTime;
    
    if (!owner.abilityActive[owner.activeWeapon]) {
        if (owner.team == TEAM_BLUE) owner._oldTauntSprite = ScoutBlueTauntS; else owner._oldTauntSprite = ScoutRedTauntS;
        owner.tauntsprite = ScoutBonkS;
        owner.tauntlength = sprite_get_number(owner.tauntsprite)/2;
        owner.tauntindex=owner.tauntlength * owner.player.team;
        owner.tauntend=owner.tauntlength * (1 + owner.player.team) - 1;
        owner.tauntspeed = 4;
    }
}
</argument>
      </arguments>
    </action>
  </actions>
</event>
